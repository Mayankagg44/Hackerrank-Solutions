#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int num,count=0;
    scanf("%d",&num);
    while (num!=0){
        num=num&(num << 1);
        count++;
    }
    printf("%d ",count);    
    return 0;
}
